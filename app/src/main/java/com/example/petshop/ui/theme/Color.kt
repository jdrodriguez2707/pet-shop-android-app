package com.example.petshop.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales para Pet Shop
val Primary = Color(0xFFFFA726)     // Naranja calido, vibrante y amigable
val PrimaryVariant = Color(0xFFFF7043) // Variante más clara del primario
val Secondary = Color(0xFF4CAF50)    // Verde para elementos secundarios
val Background = Color(0xFFF5F5F5)  // Fondo claro para mejor legibilidad
val Surface = Color(0xFFFFFFFF)     // Superficie blanca para tarjetas
val Error = Color(0xFFE53935)       // Rojo para errores
val OnPrimary = Color(0xFFFFFFFF)   // Texto blanco sobre color primario
val OnSecondary = Color(0xFF000000) // Texto negro sobre color secundario
val OnBackground = Color(0xFF121212) // Texto oscuro sobre fondo
val OnSurface = Color(0xFF121212)   // Texto oscuro sobre superficie
val OnError = Color(0xFFFFFFFF)     // Texto blanco sobre error

// Colores adicionales para categorías
val CatColor = Color(0xFFE91E63)    // Rosa para categoría de gatos
val DogColor = Color(0xFF2196F3)    // Azul para categoría de perros
val BirdColor = Color(0xFFFC3BFF)   // Amarillo para categoría de pájaros
val FishColor = Color(0xFF03A9F4)   // Azul claro para categoría de peces
val SmallPetColor = Color(0xFF9C27B0) // Morado para pequeñas mascotas
